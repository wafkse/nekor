[package]
name = "nekor-build"
version = "0.1.0"
edition.workspace = true

[dependencies]
cargo_metadata = "0.23.1"
# TODO: Remove unused features from tera.
clap = { version = "4.6.0", features = ["derive", "cargo"] }
fack = "0.1.2"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
tera = { version = "1.20.1", features = ["builtins"] }
toml = "1.1.0"

[lints]
workspace = true
