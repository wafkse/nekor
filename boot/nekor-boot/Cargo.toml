[package]
name = "nekor-boot"
version.workspace = true
edition.workspace = true

[features]
default = []
# Whether the boot protocol library should assume a kernel is being booted.
kernel = []

[dependencies]
nekor-bitwise = { workspace = true }
