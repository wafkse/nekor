[package]
name = "nekor-boot"
version = "0.1.0"
edition.workspace = true

[features]
default = []
# Whether the boot protocol library should assume a kernel is being booted.
kernel = []

[dependencies]
