# The `nekor-bitwise-extract-{N}-{M}` crate collection.

These are the internal sub-crates of the `nekor-bitwise` crate.

This is performed to divide the exhaustive `Extract` trait implementations into a set of independent crates to shorten compile times of `nekor-bitwise`.
